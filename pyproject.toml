[tool.pytest.ini_options]
minversion = "8.0"
addopts = "-ra -q"
testpaths = [
    "tests",
    "integration",
    "services"
]
DJANGO_SETTINGS_MODULE = "myservice.settings"
python_files = ["tests.py", "test_*.py", "*_tests.py"]